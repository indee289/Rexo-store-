-- Add DELETE policy for products
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;

-- Add DELETE policy for campaigns
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;

-- Ensure users can update role/status etc on users (if they are admin, but let's allow all for now since frontend handles it)
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;

-- Same for withdrawals, deposits, disputes, kyc_documents
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;

-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;

-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;

-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;
-- NEUTRALIZED FOR SECURITY: Restricted USING (true) bypass
SELECT 1;
